# MongoDB Connection
# Get this from MongoDB Atlas: https://cloud.mongodb.com
# Format: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/?retryWrites=true&w=majority

# MongoDB Database Name
MONGODB_DB_NAME=bmsview

# Google Gemini API Key
# Get this from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Gemini Model Selection (optional, defaults to flash)
GEMINI_MODEL=gemini-2.5-flash

# Log Level
LOG_LEVEL=DEBUG

# Netlify Function Timeout Configuration (optional)
# CRITICAL: Netlify has hard timeout limits based on plan tier:
# - Free: 10 seconds
# - Pro/Business: 26 seconds
# - Enterprise: Configurable (can be higher)
# 
# Set this to match your Netlify plan, minus a safety buffer:
# - Free tier: Use 8000 (8 seconds, 2s buffer)
# - Pro/Business: Use 20000 (20 seconds, 6s buffer) - DEFAULT
# - Enterprise: Contact support for custom timeout limits
#
# The system uses this to:
# 1. Save checkpoints before timeout
# 2. Enable automatic retry/resume for complex queries
# 3. Prevent "504 Gateway Timeout" errors
NETLIFY_FUNCTION_TIMEOUT_MS=20000

# CORS Configuration (optional)
# Comma-separated list of allowed origins for CORS
# If not set, defaults to localhost and Netlify deployment URLs
# ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com

# GitHub API Configuration (for AI Feedback Issue Creation)
# Personal Access Token with 'repo' scope to create issues
# Get this from: https://github.com/settings/tokens
# Required scopes: repo (or public_repo for public repositories)
GITHUB_TOKEN=YOUR_GITHUB_PERSONAL_ACCESS_TOKEN

# GitHub Repository Configuration (optional, defaults shown)
# GITHUB_REPO_OWNER=Treystu
# GITHUB_REPO_NAME=BMSview

# Note: After creating .env file, copy this to .env and replace with real values
# Never commit .env to git!
