# BMS Analyzer Local - Environment Configuration
# Copy this file to .env and fill in your values

# Server port (optional, defaults to 3847)
PORT=3847

# Note: API keys can also be configured via the Settings UI
# The UI-based settings are stored in settings.json and take precedence
